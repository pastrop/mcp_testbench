[project]
name = "long-running-agent"
version = "0.1.0"
description = "Portfolio Optimization Harness — three-agent architecture (Planner → Generator → Evaluator)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.52.2",
]

[tool.uv]
package = false
